[package]
name = "sandbox-targets"
version = "0.1.0"
edition = "2024"
description = "Plain std programs run under dowsing-sandbox (built with sancov flags by ../build-targets.sh)"

[dependencies]
dowsing-target-rt = { path = "../target-rt" }
libc = "0.2"

[profile.release]
debug = 1

[workspace]
